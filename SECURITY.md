# 安全政策

## 报告安全问题

请勿在公开 Issue 中发布 API Key、私人数据、可利用漏洞细节或包含敏感内容的项目文件。

发现安全问题时，请通过 GitHub 联系仓库所有者；如果仓库已启用 **Private vulnerability reporting**，请优先使用仓库的 **Security → Report a vulnerability** 页面。报告应包含受影响版本、复现步骤、可能影响和建议修复方式。

## 数据与凭据

- API Key 默认只保存在当前 R 会话内；
- 用户主动启用本地保存后，Key 会以可读取文本存放在当前用户的系统配置目录；
- `.easyr` 项目不应包含 API Key，但会包含用户导入的数据；
- 提交 Issue 或 PR 前，请使用模拟数据替换业务数据和个人信息；
- 不要提交 `.Renviron`、`.env`、`.easyr`、`.rds` 或本地数据目录。

## 支持范围

安全修复以当前默认分支的最新版本为准。旧版本可能不会单独维护。
